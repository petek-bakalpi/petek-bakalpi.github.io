# CLAUDE.md — פתק בקלפי 🗳️

הקשר מלא לעבודה על הפרויקט. נבנה בשיחת Claude ממושכת (יולי–אוגוסט 2026); הקובץ הזה מחליף את זיכרון השיחה.

## מהות הפרויקט
כלי אינטראקטיבי אישי־התנדבותי לקראת בחירות הכנסת ה־26 (27.10.2026), שנועד לעזור לקהל צעיר ("דור טיקטוק") להתמצא, להשוות ולהצביע. היוצר: אזרח פרטי, חבר באחת ממפלגות המחנה, תומך המחנה הדמוקרטי כולו (ארבע מפלגות: ישראל ביתנו/ליברמן, ביחד/בנט־לפיד, ישר!/איזנקוט, הדמוקרטים/גולן). הכלי מוטה במוצהר נגד גוש נתניהו — אבל מחויב ליושרה עובדתית מוחלטת.

## עקרונות בל־יעברו (לשמור בכל שינוי!)
1. **חזקת החפות** — כל אזכור חקירה/חשד מנוסח "פרסומים/חשדות", לעולם לא קביעת אשמה.
2. **🔎 בדקו בעצמכם** — כל טענה עובדתית מקבלת כפתור חיפוש גוגל. אמינות = הזמנה לאימות.
3. **דעה ≠ עובדה** — דעות מסומנות ("דעת יוצר הכלי"), כולל בדיסקליימר.
4. **מבחן המחלוקת לשאלון** — שאלה חדשה חייבת שיהיה עליה צד־נגד לגיטימי בתוך המחנה. אסור "אתם בעד דברים טובים?" (מתנת נקודות למפלגה).
5. **איזון מדיד** — אחרי כל שינוי בשאלון להריץ סימולציה (ראו QA): כל 4 המפלגות חייבות להישאר ברות־השגה.
6. **תיקון מהיר** — טעות שמתגלה מתוקנת מיד; זה חלק מהמותג.
7. **אפס אחסון** — אין שרת, אין עוגיות, **אסור localStorage** (הבטחת פרטיות מפורשת בדיסקליימר). שיאים/נעילות חיים רק בזיכרון הסשן.
8. **בכל מחיר: אין להשתמש בגרש כפול `"` בתוך מחרוזות עברית ב־JS** — משתמשים ב־`״` (גרשיים עבריים) ובגרש בודד `'` לאטריביוטים ב־HTML פנימי.

## ארכיטקטורה
- **קובץ יחיד**: `index.html` (~300KB) — HTML+CSS+JS, RTL, פונטים Secular One/Heebo מגוגל.
- ניווט: 3 טאבים תחתונים (ראשי/משחקים/תכל'ס), ~23 מסכים (`<section class="screen">`), פונקציית `show(sc)` מסתירה את כולם ומציגה אחד; `goBack()` לפי `lastTab`.
- קבועי גרסה בראש הסקריפט: `APP_VERSION`, `APP_UPDATED`, `ELECTION_DATE` — לעדכן בכל דיפלוי.
- פלטת צבעים: ink `#0F1530`, blue `#3D7BFF`, mint `#33D69F`, coral `#FF5964`, gold `#F5B841`.

## מערכי הנתונים (מקורות אמת יחידים)
| מערך | גודל | מזין | מבנה |
|---|---|---|---|
| `QUICK`/`DEEP` | 11/21 | שאלון השידוך | `{e,x,q,w:{},n:{},m:{}}` — משקלות למפתחות lib/yachad/yashar/dem; `EXPLAIN[x]` מסבירונים |
| `GOVPOOL` | 63 | הקימו ממשלה, נחשו מי, מנדטל | `{n,p:D/Y/B/L,g:f/m,tg:[sec/econ/law/edu/health/soc/dip],b,pos,rl?,lead?}` — `pos`=מקום ברשימה (אמיתי בדמוקרטים, מוערך באחרות), `rl:0`=לא ריאלי (סקר+5 נורבגי) |
| `FACTS` | 7 | מחלקת אקטואליה + סיפורי המרוץ | `{e,t,claim,said,twist,bottom,srch,race?:{h,parts:[3]}}` — פריט עם `race` נכנס אוטומטית לרוטציית המרוץ |
| `HL_ITEMS` | 14 | יותר-או-פחות + עמוד המספרים | `{t,sub,v(מיליוני ₪),d,src,q}` — מופיע אוטומטית בשניהם |
| `QUOTES` | 21 | מי אמר את זה | `{q,a,ctx,srch}` — רק ציטוטים מתועדים; ספקולציה מסומנת "מיוחס" |
| `DICT_DEM` | 4 | מילון המפלגות | `{idc,leader,record,lst,pro[],con[],kn(HTML),mz(HTML)}` — kn=רשימה לכנסת, mz=תקציר מצע+קישור |
| `CORE_ISSUES` | 21 | על מה רבים | `{e,t,body,why,deep}` — deep=העמקה מול טיעוני נגד (3 מקטעים) |
| `LEDGER` | 29 | פנקס הכישלונות | `{e,t,body,q,cmp?}` — cmp=השוואת ממשלת השינוי (5 קיימות) |

## המשחקים (9)
🎲 מתחדשים: נחשו מי (bank מ־GOVPOOL, 4 סוגי שאלות; שאלות-סדר רק על הדמוקרטים כי הסדר אמיתי) · מי אמר · יותר-או-פחות · מנדטל (חידה יומית דטרמיניסטית מתאריך; חיפוש לפי שם מפלגה) · המרוץ לקלפי (סיפור אקראי מ-FACTS ב-3 עצירות, פרק 3 בקו הסיום; מצב אינסופי נפתח בניצחון).
🎯 קלאסיקות: השידוך (מצב חי בקלפי, שקיפות מלאה, שומר rawTop<2) · משחק הקואליציות · הקימו ממשלה (רה"מ לפי סדר סקרים; בורר עם חיפוש+סינון+מיון-לפי-pos) · מטה הבחירות (קליקר; total≠bank).

## תחזוקה שוטפת — פלייבוק
- **פריט אקטואליה חדש**: להוסיף ל-FACTS (עם race אם מתאים למרוץ). לאמת ציטוטים בחיפוש לפני!
- **סקרים חדשים**: לעדכן סדר רה"מ במשחק הממשלה, דגלי `rl` (מנדטים+~5 נורבגי), אזכורי "הגדולה" במילון.
- **ספטמבר — הגשת רשימות**: לעדכן סדר אמיתי ב-GOVPOOL (pos) לכל המפלגות, להסיר את כרטיס "הרשימות טרם הוגשו" במילון, לעדכן kn.
- **שינוי שאלון**: מבחן המחלוקת → עדכון תוויות מספר-שאלות → הרצת סימולציית איזון.
- **דיפלוי**: לעדכן APP_VERSION/APP_UPDATED → commit+push של index.html → GitHub Pages מתעדכן תוך דקה. Changelog ב-README.

## QA לפני כל דיפלוי
```bash
# חילוץ הסקריפט ובדיקת תחביר
python3 -c "import re;s=open('index.html',encoding='utf-8').read();open('/tmp/c.js','w',encoding='utf-8').write(re.search(r'<script>([\s\S]*)</script>',s).group(1))" && node --check /tmp/c.js
```
בדיקות ספירה: QUICK=11, DEEP=21, GOVPOOL=63 (כולם עם pos), FACTS=7, HL=14, QUOTES=21, DICT: kn/mz/idc/leader/record/lst ×4, CORE=21 (+21 deep), LEDGER cmp=5.
**סימולציית איזון שאלון** (חובה אחרי שינוי משקלות): לבנות סקריפט שמחלץ QUICK/DEEP, מריץ exhaustive על המהיר ו-300K מדגם על המעמיק, ובודק: כל מפלגה זוכה באחוז סביר מהמרחב + "פרופיל אדבוקט" (בחירה חמדנית פר-שאלה) מנצח לכל מפלגה במרווח>1.

## דפוסי עריכה (חשוב!)
- עריכות בפייתון heredoc עם `assert old in s` לפני כל replace — לעולם לא sed עיוור.
- זהירות מ-`${}` בתוך תבניות JS כשמריצים דרך bash — להשתמש ב-heredoc מצוטט (`<< 'EOF'`).
- באגים שכבר תוקנו ואסור להחזיר: לולאות rAF כפולות (המרוץ), מערכות צירים מעורבות בהתנגשויות, איפוס ויזואלי של המצב החי, `show()` שנשכח.

## דיפלוי ותשתית
- ריפו: github.com/petek-bakalpi/petek-bakalpi.github.io · אתר: petek-bakalpi.github.io (Pages מ-main root).
- `og-image.png`: לחידוש נדרש python-bidi (`bidi.algorithm.get_display`) — בלעדיו העברית מתהפכת.
- דומיין עתידי: לעדכן `og:url`/`og:image` בקובץ + CNAME.
- טוקנים: fine-grained, מוגבל לריפו, תוקף קצר, למחוק אחרי שימוש.
